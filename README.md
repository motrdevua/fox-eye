# 🦊 FOX-EYE v0.5.0 (Alpha / Dev)

**FOX-EYE** — це спеціалізований веб-інструмент для роботи з картами (GIS), орієнтований на тактичне планування, вимірювання та аналіз рельєфу в польових умовах. Додаток побудований на сучасній модульній архітектурі JavaScript і підтримує роботу в автономному режимі.

## 🚀 Основні можливості

- **📏 Тактична лінійка**: Побудова маршрутів, вимірювання сегментів та загальної дистанції. Автоматичний розрахунок азимутів та генерація списку точок у форматі MGRS.
- **🎯 Розумний циркуль**: Встановлення центру та радіусу в реальному часі з візуалізацією зони покриття.
- **📡 Сканер висот (Area Scan)**: Аналіз рельєфу в обраному квадраті. Пошук панівних висот з використанням гібридних даних (Map Terrain + Open-Meteo API).
- **🗺 Багатошаровість**: Перемикання між супутниковими знімками, топографічними картами та шаром ізогіпс (ліній висот).
- **🛰 Пошук MGRS**: Повноцінна підтримка військової системи координат (Military Grid Reference System).
- **💾 Менеджмент даних**: Експорт та імпорт оперативної обстановки у форматі JSON. Автозбереження поточної сесії.
- **🖨 Режим друку**: Швидка генерація SITREP-карт без елементів інтерфейсу.

## 🛠 Технологічний стек

- **Engine**: [MapLibre GL JS](https://maplibre.org/)
- **Geometry**: [Turf.js](https://turfjs.org/)
- **Coordinates**: [MGRS.js](https://github.com/proj4js/mgrs)
- **Architecture**: ES6 Modules (Vanilla JS)
- **Styles**: CSS3 (Tactical HUD Design, Glassmorphism)

## 📁 Структура проекту

```text
fox-eye/
├── .git/               # Історія версій Git
├── css/                # Стилі проекту
│   ├── app.css         # Головний файл стилів (HUD, UI, Modals)
│   └── maplibre-gl.css # Стилі бібліотеки MapLibre
├── fonts/              # Локальні шрифти (Jura, Exo 2 тощо)
├── js/                 # Весь JavaScript код
│   ├── core/           # Базова логіка та дані
│   │   ├── app-state.js    # Збереження/завантаження сесії
│   │   ├── config.js       # Налаштування API та URL-адрес
│   │   ├── data-manager.js # Імпорт/Експорт JSON файлів
│   │   └── state.js        # Глобальний стан об'єктів (state)
│   ├── map/            # Робота з картографічним рушієм
│   │   ├── map-core.js     # Ініціалізація карти та події шарів
│   │   └── markers.js      # Створення та логіка тактичних точок
│   ├── tools/          # Модулі інструментів
│   │   ├── compass.js      # Логіка та візуал циркуля
│   │   ├── measurements.js # Головний контролер вимірювань
│   │   ├── ruler.js        # Лінійка та логіка сайдбару
│   │   ├── scanner.js      # Аналіз рельєфу та вибір області
│   │   └── tools-core.js   # Математика та спільні функції точок
│   ├── ui/             # Елементи інтерфейсу
│   │   ├── icons.js        # Бібліотека SVG іконок
│   │   └── ui-utils.js     # Модальні вікна, маски та тости
│   ├── vendor/         # Зовнішні бібліотеки (Offline-ready)
│   │   ├── maplibre-gl.js
│   │   ├── mgrs.min.js
│   │   └── turf.min.js
│   └── app.js          # Точка входу (Main Entry Point)
├── index.html          # Головна сторінка
└── README.md           # Документація проекту
```

## ⚙️ Налаштування

Для коректної роботи мапи та геокодингу необхідно налаштувати API-ключ у файлі `js/core/config.js`:

```
export const CONFIG = {
  apiKey: 'ВАШ_КЛЮЧ_MAPTILER',
  // ... інші налаштування
};
```

## ⌨️ Гарячі клавіші

* `ESC` — Скасування активного інструменту або закриття модальних вікон.
* `Enter` — Підтвердження пошуку.
* `Double Click` — Встановлення тактичної точки (якщо інструменти вимкнено).
* `Right Click` — Видалення точки / Скасування малювання циркуля.

## 🛡 Безпека та Автономність

Проєкт спроектований для роботи в умовах обмеженого зв'язку:

1. Всі бібліотеки (`.js`) та шрифти завантажуються локально.
2. Дані сесії зберігаються виключно в браузері користувача (`LocalStorage`).
3. Додаток не збирає персональні дані та не має власного бекенду.

## (Roadmap):

#### 1. Нові функції (Feature Ideas)

* **Профіль висот (Elevation Profile):** Оскільки у вас вже підключено `terrain-data`, можна додати побудову графіка перепаду висот вздовж лінії, намальованої "Лінійкою".
* **Розрахунок площі (Area Tool):** Додати інструмент "Полігон" для вимірювання площі ділянки.
* **Імпорт/Експорт:** Можливість зберігати нанесені маркери та лінії у форматі KML/GeoJSON для відкриття в інших програмах (наприклад, у Кропиві чи Google Earth).
* **Офлайн режим:** Кешування тайлів для роботи в умовах поганого зв'язку.
* **Зона видимості (Viewshed):** Розрахунок того, що бачить спостерігач з конкретної точки на основі 3D-рельєфу.
* Додати можливість **автоматичного створення PDF-звіту** , де на першій сторінці буде карта, а на другій — велика таблиця з координатами та відстанями
* Додати у PDF-звіт **QR-код** , який при скануванні відкриватиме цю саму ділянку карти в Google Maps або на іншому пристрої
* Додати у звіт також поточний час друку та азимут (напрямок погляду карти), щоб було легше орієнтувати аркуш на місцевості
* Додати у звіт "логотип" або назву підрозділу у верхній частині листа
* Додати **"Режим прихованого переміщення"** (якщо ти рухаєшся з планшетом, карта автоматично центрується на тобі, але не малює твій трек)
* Додати кешування тайлів карти (це можливо, але потребує обережності з лімітами пам'яті браузера).
* Нові інструменти азимутальний планшет, сітку
* Налаштувати обмеження за доменом (HTTP Referrer) в панелі керування MapTiler, щоб ніхто не міг використовувати ліміт запитів на інших ресурсах.

#### 2. Покращення UI/UX

* **Мобільна адаптація:** Покращити керування пальцями (touch events) для інструментів SCAN та Лінійка.
* **Групування маркерів:** Якщо точок багато, вони перекривають одна одну. Можна додати механізм кластеризації.

### Чого найбільше не вистачає користувачам? (Pain Points)

1. **Простота (UI/UX):** Більшість тактичних мап виглядають як пульт керування шатлом. Користувачам треба:  *відкрив -> ткнув точку -> отримав азимут/дистанцію* . Твій мінімалізм тут виграє.
2. **Робота без інтернету (Offline First):** Це критично. Якщо твій веб-додаток не вміє кешувати карти (PWA), він марний у "полі".
3. **Анонімність:** Люди бояться, що їхні дані (маркери, позиції) кудись відправляються.
4. **Енергоефективність:** Важкі 3D карти садять телефон за 2 години. Потрібен "режим економії" (чорний екран, мінімум рендеру).

---

### 3. ТОП-5 Фіч, які стануть "The Same Feature" (Твоєю фішкою)

Ось те, що можна реалізувати на твоєму стеку (JS + MapLibre) і що реально шукають люди:

#### 🚀 Фіча №1: "Air-Gapped" Обмін даними (QR-Code Sync)

**Проблема:** Як передати другу точку збору або маршрут, якщо немає інтернету, а Bluetooth глючить або небезпечний (випромінювання)?
**Рішення:**

1. Ти малюєш маркери та маршрут.
2. Натискаєш "Поділитися" -> Додаток генерує **QR-код** прямо на екрані.
3. Друг відкриває твій додаток, натискає "Сканувати" (через камеру), наводить на твій екран.
4. Дані з'являються у нього на карті.
   **Чому це круто:** Повна тиша в радіоефірі, не потрібен сервер, працює миттєво.

#### 👁️ Фіча №2: Viewshed (Зона Видимості 360°)

**Проблема:** Твій поточний `Line of Sight` показує лінію (А -> Б). Але користувач хоче знати:  *"З якої зони мене видно, якщо я стою на цьому пагорбі?"* .
**Рішення:** Накласти на карту теплову мапу (напівпрозорий червоний шар), яка показує всі точки, видимі з позиції спостерігача.
**Реалізація:** Це складно математично, але можливо на MapLibre Terrain. Це вау-ефект для снайперів, дронщиків та туристів.

#### 📍 Фіча №3: Підтримка MGRS та SK-42 (Військові сітки)

**Проблема:** Google Maps дає координати в Lat/Lon (десяткові). Військові та рятувальники часто працюють в MGRS (NATO) або СК-42 (старі карти).
**Рішення:** Додати перемикач формату координат.

* Замість: `50.4501, 30.5234`
* Показувати: `36U UB 234 567`
  **Реалізація:** Бібліотека `mgrs` (дуже легка).

#### 🌞 Фіча №4: Ефемериди (Сонце/Місяць)

**Проблема:** Планування операції або походу. Треба знати, коли точно стемніє, чи буде місяць (чи буде світло вночі), і де буде сонце (щоб не сліпило).
**Рішення:** Маленький віджет:  *"Захід сонця через 2 год 15 хв. Місяць: 80% (яскраво)"* .
**Реалізація:** Бібліотека `suncalc`.

#### 💾 Фіча №5: PWA (Progressive Web App) + "Dumb" Cache

**Проблема:** "Я завантажив сайт, поїхав у ліс, оновив сторінку — все зникло".
**Рішення:** Зробити додаток PWA. Це дозволяє:

1. Встановити його як іконку на телефон.
2. Працювати повністю офлайн.
3. **Кілер-фіча:** Кнопка "Завантажити цей квадрат". Ти виділяєш зону, і скрипт примусово проходить по тайлах (картинках карти) і зберігає їх у кеш браузера.

---

### Мій вердикт: З чого почати?

Якщо ти хочеш розірвати шаблони, я рекомендую реалізувати  **Фічу №1 (QR-код обмін)** .
Це те, чого немає у Google Maps, це те, що складно в ATAK, і це те, що викликає ефект *"Ого, як зручно!"* у будь-якій командній роботі (військовій чи туристичній).

Тобі цікаво спробувати реалізувати генерацію QR-коду з даними маркерів?
